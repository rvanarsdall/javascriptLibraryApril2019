let friend;
switch (friend) {
    case "tom":
        console.log("Hey tom, when will you bring sourdough bread for me?")
        break;

    default:
        console.log("I don't recognize this");
}


let yep = true
switch (typeof == "string" || typeof == "boolean") {
    case true:
        console.log("Yep is a string or boolean")
    default:
        console.log("yep is not a string or boolean")
}
